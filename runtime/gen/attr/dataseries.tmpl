{{ if not .IsReadOnly }}
w.{{ .StarlarkGoName }} = {{ .StarlarkName }}
if val, err := DataSeriesFromStarlark({{ .StarlarkName }}); err == nil {
	w.{{ .GoPath }} = val
} else {
	return nil, err
}
{{- end }}
