{{ if not .IsReadOnly }}
w.starlark{{ .GoName }} = {{ .StarlarkName }}
if val, ok := starlark.AsFloat(w.starlark{{ .GoName }}); ok {
	w.{{ .GoPath }} = val
} else if w.starlark{{ .GoName }} != nil {
	return nil, fmt.Errorf("expected number, but got: %s", w.starlark{{ .GoName }}.String())
}
{{- end }}
