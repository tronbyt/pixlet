{{ if not .IsReadOnly }}
w.{{ .StarlarkGoName }} = {{ .StarlarkName }}
if val, err := WeightsFromStarlark({{ .StarlarkName }}); err == nil {
	w.{{ .GoPath }} = val
} else {
	return nil, err
}
{{- end }}
